language: java

sudo: required

before_install:
  - wget http://www-us.apache.org/dist/thrift/0.9.3/thrift-0.9.3.tar.gz
  - tar xfz thrift-0.9.3.tar.gz
  - cd thrift-0.9.3 && ./configure --enable-libs=no --enable-tests=no --enable-tutorial=no && sudo make install && cd ..